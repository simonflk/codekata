// @ts-check
import { makeTest } from './test';

//? Set to `true` when you are ready to enable tests
const RUN_TESTS = false;
makeTest(searchKFromEnd, RUN_TESTS);

/**
 * @typedef {Node|null} MaybeNode
 *
 * @typedef {Object} Node
 * @property {number} data
 * @property {MaybeNode} next
 */

/**
 * @param {MaybeNode} linkedList
 * @param {number} k
 * @returns {number|null}
 */
function searchKFromEnd(linkedList, k) {
  // Magic goes here
  return 0;
}
