// @ts-check
import { makeTest } from './test';

//? Set to `true` when you are ready to enable tests
const RUN_TESTS = false;
makeTest(exchangeSort, RUN_TESTS);

/**
 * 
 * @param {number[]} sequence 
 * @returns {number}
 */
function exchangeSort(sequence) {
  //coding and coding..
  return -1;
}