// @ts-check
import { makeTest } from './test';

//? Set to `true` when you are ready to enable tests
const RUN_TESTS = false;
makeTest(pointVsVector, RUN_TESTS);

/**
 * @param {number[]} point
 * @param {[[number, number], [number, number]]} vector
 */
function pointVsVector(point, vector) {
  return 0;
}
